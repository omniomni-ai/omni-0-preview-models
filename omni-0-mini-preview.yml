# mergekit-yaml ./omni-0-mini-preview.yml ./omni-0-mini-preview
# Merging configuration for omni-0-mini-preview. Placeholders for subdomain-specific experts.

models:

  - model: science-expert
    parameters:
      density: 0.5
      weight: 0.8
      
  - model: technology-expert
    parameters:
      density: 0.5
      weight: 0.8
      
  - model: engineering-expert
    parameters:
      density: 0.5
      weight: 1.0
      
  - model: math-expert
    parameters:
      density: 0.5
      weight: 1.0

  - model: base-model
    parameters:
      density: 0.5
      weight: 0.8

merge_method: dare_ties
base_model: base-model

parameters:
  normalize: true

dtype: bfloat16
